<configuration>
    <property name="LOG_DIR" value="${bettercli.log.dir:-${user.home}/.bettercli/logs}"/>
    <property name="LOG_LEVEL" value="${bettercli.log.level:-INFO}"/>
    <property name="LOG_MAX_HISTORY" value="${bettercli.log.maxHistory:-7}"/>
    <property name="LOG_MAX_FILE_SIZE" value="${bettercli.log.maxFileSize:-10MB}"/>
    <property name="LOG_TOTAL_SIZE_CAP" value="${bettercli.log.totalSizeCap:-100MB}"/>

    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/bettercli.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/bettercli.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${LOG_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${LOG_TOTAL_SIZE_CAP}</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <root level="${LOG_LEVEL}">
        <appender-ref ref="ROLLING_FILE"/>
    </root>
</configuration>
